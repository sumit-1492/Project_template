from setuptools import setup, find_packages
from typing import List

PROJECT_NAME = "Machine Learning Project"
VERSION = '0.0.1'
AUTHOR = "SUMIT KUMAR"
DESCRIPTION = "ML project"


setup(
    name = PROJECT_NAME,
    version= VERSION,
    author = AUTHOR,
    description= DESCRIPTION,
    packages= find_packages(), ## main init file our project folder
    install_requires = 
)
